#pragma once
#ifndef GROUND_FLOOR_WC_H
#define GROUND_FLOOR_WC_H

#include <windows.h>
#include <GL/glut.h>

// Hàm vẽ toàn bộ không gian WC tầng trệt
void drawGroundFloorWC(float x, float y, float z);

#endif